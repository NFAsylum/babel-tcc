# Give Gradle + the IntelliJ SDK enough heap for the sandbox build.
org.gradle.jvmargs=-Xmx2048m -XX:+UseParallelGC
org.gradle.caching=true
# Let the IntelliJ plugin pull the Kotlin stdlib matching the target IDE.
kotlin.stdlib.default.dependency=false
