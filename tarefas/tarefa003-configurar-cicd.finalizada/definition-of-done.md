# Definition of Done - Tarefa 003

- [ ] Workflow `build-core.yml` roda `dotnet build` e `dotnet test` em push e PR
- [ ] Workflow `build-vscode.yml` roda `npm install`, `npm run build`, `npm run lint`
- [ ] Workflow `test.yml` roda em PRs para main
- [ ] Badges de status do CI aparecem no README
- [ ] Todos os workflows passam no primeiro push
